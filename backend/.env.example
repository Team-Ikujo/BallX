# ============================================
# GoTi 백엔드 환경변수 설정
# ============================================
# 사용법:
#   cp .env.example .env
#   .env 파일에서 빈 값들을 채워주세요
# ============================================

# --- 서버 ---
SERVER_PORT=8080

# --- 데이터베이스 ---
# IDE(로컬) 실행 시: localhost 사용
# Docker 실행 시: docker-compose.yml에서 자동 오버라이드
DATASOURCE_URL=jdbc:postgresql://localhost:5432/ballx_local
DATASOURCE_USERNAME=goti
DATASOURCE_PASSWORD=goti1234

# PostgreSQL 컨테이너 설정 (docker-compose용)
POSTGRES_USER=goti
POSTGRES_PASSWORD=goti1234

# --- JWT ---
# 시크릿 키: 최소 256비트(32자) 이상의 Base64 인코딩 문자열
# 생성 방법: openssl rand -base64 64
JWT_SECRET_KEY=your-jwt-secret-key-base64-encoded
JWT_ACCESS_TIME=1800000
JWT_REFRESH_TIME=604800000

# --- 로깅 ---
# JSON 로깅 활성화: logstash / 비활성화: 빈 값
LOG_FORMAT=logstash
LOG_LEVEL=INFO

# --- OAuth: 카카오 ---
# https://developers.kakao.com 에서 앱 등록 후 발급
KAKAO_REDIRECT_URI=http://localhost:8080/oauth/kakao/callback
KAKAO_CLIENT=your-kakao-rest-api-key
KAKAO_SECRET=your-kakao-client-secret

# --- OAuth: 네이버 ---
# https://developers.naver.com 에서 앱 등록 후 발급
NAVER_REDIRECT_URI=http://localhost:8080/oauth/naver/callback
NAVER_CLIENT=your-naver-client-id
NAVER_SECRET=your-naver-client-secret

# --- OAuth: 구글 ---
# https://console.cloud.google.com 에서 OAuth 2.0 클라이언트 ID 발급
GOOGLE_REDIRECT_URI=http://localhost:8080/oauth/google/callback
GOOGLE_CLIENT=your-google-client-id.apps.googleusercontent.com
GOOGLE_SECRET=your-google-client-secret
